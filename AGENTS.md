# BYG 仓库协作规则

BYG 是本次行动的代号，仓库根目录为本文件所在目录。

## 范围与结构

- BYG 统一版本管理，408 与 math 保持独立的学科内容和状态主表。
- 进入子项目先读该目录的 AGENTS.md。根目录规则不能把不同学科的教学状态合并。
- 根目录 Git 操作不意味着允许改写其他任务的教学材料；只处理本次授权内容。

## Git 安全

- 先查看 git status、当前分支、远程和变更，再操作；不覆盖用户或其他任务的未提交修改。
- 用户已授权公开推送 `408/` 和 `math/` 子项目的有效学习资料；精确选择提交文件，仍排除本地压缩备份、临时文件、机器配置和凭据。
- 2026-09-18用户已明确确认上述学习资料（含原始作答、预测、批改、掌握度记录）的公开上传，无需每次重复询问；新出现的凭据、第三方敏感资料或超出BYG项目的内容不在授权范围。
- 不添加凭据、密钥、环境文件、临时资产与压缩备份；不改变全局 Git 身份或配置。
- 不强推，不擅自重写历史，不用 reset --hard 或 clean 清除未知改动。
- 用户已要求后续BYG有效内容变更后提交并推送；每次修改任务完成后检查本任务变更、提交、推送到origin/main，并核对远程提交。没有有效变更不创建空提交。
- 多任务并行时只提交本任务相关且已完成的修改，不盲目吞入其他任务的进行中改动；本次全项目初次导入是用户明确授权的例外。
- 推送失败要明确报告“本地已提交、远程未同步”及原因；不得谎称成功、强推或擅自覆盖远程历史。该规则是任务内同步，不是常驻文件监听器，不设置脱离任务的后台推送。

## 证据维护

- 保留真实作答、题面和事前预测，不倒填预测，不虚构日期、得分或完成状态。
- 修改状态主表时同步适用的派生看板，并检查节点ID、题目ID、时间预算和链接。
- Git 提交成功不代表学习任务完成，推送成功不代表知识掌握。
