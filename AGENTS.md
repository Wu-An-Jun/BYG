# BYG 仓库协作规则

BYG 是本次行动的代号，仓库根目录为本文件所在目录。

## 范围与结构

- BYG 统一版本管理，408 与 math 保持独立的学科内容和状态主表。
- 进入子项目先读该目录的 AGENTS.md。根目录规则不能把不同学科的教学状态合并。
- 根目录 Git 操作不意味着允许改写其他任务的教学材料；只处理本次授权内容。

## Git 安全

- 先查看 git status、当前分支、远程和变更，再操作；不覆盖用户或其他任务的未提交修改。
- 精确选择提交文件。已有个人学习记录的公开上传尚未确认；本次首次推送只包含根目录 README、AGENTS、.gitignore、.gitattributes。
- 原始作答、预测、批改与个人信息上传公开仓库前，必须得到相应公开范围确认。
- 不添加凭据、密钥、环境文件、临时资产与压缩备份；不改变全局 Git 身份或配置。
- 不强推，不擅自重写历史，不用 reset --hard 或 clean 清除未知改动。
- 只有用户要求时才推送；不因普通教学活动自动提交、推送或设置后台任务。

## 证据维护

- 保留真实作答、题面和事前预测，不倒填预测，不虚构日期、得分或完成状态。
- 修改状态主表时同步适用的派生看板，并检查节点ID、题目ID、时间预算和链接。
- Git 提交成功不代表学习任务完成，推送成功不代表知识掌握。
